#

test_that("should be caught in strategy consstructor", {
  ##TODO:
  
  # test_strategy_single_exp <- list(
  #   marginal_distributions = c("norm", "exp"), # Corresponds to age, X1
  #   marginal_params = list(
  #     list(mean = 55, sd = 8),    # Parameters for 'age' (normal)
  #     list(rate = 0.04)           # Parameters for 'X1' (exponential)
  #   )
  # )
  # 
  # result <- simulate_ALD_pseudo_pop(
  #   formula = test_formula_two_covariates,
  #   ipd = mock_ipd_robust,
  #   ald = mock_ald_long,
  #   trt_var = "trt",
  #   N = 1000,
  #   marginal_distributions = test_strategy_single_exp$marginal_distributions,
  #   marginal_params = test_strategy_single_exp$marginal_params
  # )
  
  ##TODO:
  # outstandR(marginal_distns =  c("norm", "exp"),
  #           marginal_params = list(
  #     list(mean = 55, sd = 8),
  #     list(rate = 0.04)
  #   )
  
  # expect_
})
