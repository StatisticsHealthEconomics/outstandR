# get study treatment names for each

# reference / common / anchoring treatment is last in list

#
get_ipd_trts <- function(ipd, ref_trt = "C") {
  
}

#
get_ald_trts <- function(ald, ref_trt = "C") {
  
}
